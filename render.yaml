services:
  - type: worker
    name: altin-bildirim-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python altin_bildirim_bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
